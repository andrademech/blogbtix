import styled from 'styled-components';

export const Container = styled.div`
  max-width: 80%;
  min-width: none;
  margin: 0 auto;
  background-color: var(--white);
  padding: 20px;
  font-size: 0.5rem;
  margin-top: 15px;
`;
